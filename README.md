# RandomMouseMover MFC
Turn On moves the mouse every 30 seconds and keeps the display awake. Turn Off stops it.
Upload all files, including .github, to the repository root. Then run Build MFC EXE in Actions.
